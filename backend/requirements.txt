# Framework Django
Django>=6.0
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
python-decouple>=3.8

# Base de données SQL Server (production)
mssql-django>=1.4; sys_platform != 'unsupported'

# Médias / images
Pillow>=10.0

# ── Modules IA ─────────────────────────────────────────────
# Extraction PDF
pdfplumber>=0.11
PyMuPDF>=1.24

# NLP / BERT (optionnel — lourd, ~500 MB de modèles au premier run)
transformers>=4.40
torch>=2.2
sentence-transformers>=3.0
